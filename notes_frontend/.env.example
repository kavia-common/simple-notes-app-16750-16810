# Example environment variables for Simple Notes Frontend (Nuxt)
# The orchestrator will populate these values in the actual .env.
# Do not commit real secrets here.

# PUBLIC: Base URL for the backend notes API (if/when connected)
NUXT_PUBLIC_NOTES_API_BASE_URL=https://your-notes-api.example.com
